services:
  - type: web
    name: wizardbot
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node index.js
    autoDeploy: true
    envVars:
      - key: DISCORD_TOKEN
        sync: false
      - key: CLASH_TOKEN
        sync: false
      - key: MONGO_URI
        sync: false
